# Only server and database are configured here.
# All other settings are configured via the web setup wizard on first launch.

server:
  host: "0.0.0.0"
  port: 8880

database:
  driver: "sqlite3"
  dsn: "ccmate.db?_fk=1&_journal=WAL"
